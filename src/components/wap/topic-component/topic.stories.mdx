import { Meta, Props, Story, Preview } from '@storybook/addon-docs/blocks'
import { action } from '@storybook/addon-actions'
import topic from './index.vue'
import { withKnobs, number, object, boolean, text, select, date, array, color } from '@storybook/addon-knobs'
import SelectionData from '@/mock/select-mock'

<Meta title="wap业务组件|整合题目" component={topic} decorators={[withKnobs({ escapeHTML: false })]} />

# 整合题目

<Props of={topic} />

默认-解析底部

<Preview>
  <Story name="默认">
    {{
      components: { topic },
      props: {
        controlShowBottom: { default: boolean('是否显示底部内容', false) },
        dataSubject: { default: object('题目数据', SelectionData) },
      },
      template: `<topic :dataSubject="dataSubject" :controlShowBottom="controlShowBottom">
         <div slot="questionBottom">这是自定义教师端统计插槽!!!</div>
         </topic>`,
    }}
  </Story>
</Preview>

默认-自定义底部

<Preview>
  <Story name="自定义底部">
    {{
      components: { topic },
      props: {
        controlShowBottom: { default: boolean('是否显示底部内容', false) },
        dataSubject: { default: object('题目数据', SelectionData) },
      },
      template: `<topic :dataSubject="dataSubject" :controlShowBottom="controlShowBottom">
          <div slot="bottom">自定义底部解析！！！</div>
           <div slot="questionBottom">这是自定义教师端统计插槽!!!</div>
        </topic>`,
    }}
  </Story>
</Preview>
